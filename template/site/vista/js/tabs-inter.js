/* ------------------------------------------------------------------------
	Do it when you're ready dawg!
------------------------------------------------------------------------- */

	
/*
tabs-inter = {
  init : function(){
   $('.tabs-inter').each(function(){

    var th=$(this),
     tContent=$('.tab-content-inter',th),
     navA=$('ul.nav-inter a',th)

    tContent.not().hide();
	tab_act=$('ul.nav-inter .selected-inter a').attr('href');
	$(tab_act).show();

    navA.click(function(){
				alert("click2");

     var th=$(this),
      tmp=th.attr('href')
     tContent.not($(tmp.slice(tmp.indexOf('#'))).fadeIn(600)).hide()
	 $(th).parent().addClass('selected-inter').siblings().removeClass('selected-inter');
     return false;
    });
   }
   );

  }
 }
*/
